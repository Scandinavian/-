def power(a, n):
    if n > 0 and n % 2 == 0:
        return (a ** 2) ** (n / 2)
    elif n > 0 and n // 2 != 0 or n // 2 < 1:
        return a * (a ** (n-1))
    return 1


a1 = float(input())
n1 = int(input())
print(power(a1, n1))
